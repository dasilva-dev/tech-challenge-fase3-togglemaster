apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: togglemaster
  labels:
    app: analytics-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8005
      targetPort: 8005
  selector:
    app: analytics-service
